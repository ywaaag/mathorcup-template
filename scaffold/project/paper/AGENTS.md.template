# AGENTS.md — 论文脑协议

## Runtime Context
- Competition: `{{COMPETITION_NAME}}`
- Container: `{{CONTAINER_NAME}}`
- Image: `{{IMAGE_NAME}}`
- Workspace: `{{HOST_DIR}}`
- Runtime baseline source:
  - `../spec/runtime_contract.md -> ## Current Host / Container Facts`
  - `../spec/runtime_contract.md -> ## Reference Image Environment Snapshot`

## Role Boundary
- Role: paper brain only (LaTeX writing, structure, references, figure/table insertion).
- Never edit `../src`, `../cpp`, `../notebooks`.
- Read-only upstream: `../../MEMORY.md`, `../output/`, `../figures/`, indexed handoffs.

## Mandatory Read Order
1. Read this file.
2. Read `../../MEMORY.md`.
3. Read `../spec/runtime_contract.md`.
4. Read `spec/paper_runtime_contract.md`.
5. Read `../spec/multi_agent_workflow_contract.md`.
6. If this task is claimed or delegated, read `../spec/agent_roles.json`, `../runtime/task_registry.json`, and `../runtime/work_queue.json`.
7. Before consuming handoffs, run the default intake guard: `bash ../../scripts/check_handoff_intake.sh --target ../..`.
8. Use `bash ../../scripts/check_handoff_intake.sh --latest --target ../..` or `bash ../../scripts/check_handoff_intake.sh --files --target ../..` only as indexed-path shortcuts.
9. Read the latest indexed handoff docs listed in `../../MEMORY.md`.
10. If this task is delegated or multi-session, read the relevant templates under `../workflow/`.

## Input Contract
- Only consume outputs that are indexed in `## Handoff Index`.
- Default intake command: `bash ../../scripts/check_handoff_intake.sh --target ../..`.
- Latest indexed handoff shortcut: `bash ../../scripts/check_handoff_intake.sh --latest --target ../..`.
- Indexed handoff files shortcut: `bash ../../scripts/check_handoff_intake.sh --files --target ../..`.
- Shortcut modes print only indexed paths to stdout and send warnings to stderr; they are not substitutes for the default paper-side intake guard.
- Unindexed `../output/handoff/P*.md` files may exist on disk; they are warnings only and must not enter the default paper-side intake set.
- If data or figure is missing, stop speculative writing and return a blocker or worker packet.
- `project/paper/runtime/paper.env` is the single source for active paper entrypoint, build target, and acceptance artifacts.
- `bash ../../scripts/check_handoff_intake.sh --target ../..` is the required paper-side intake guard before consuming handoffs; unindexed disk handoffs are warnings only.
- Main-brain review/done decisions must inspect indexed intake before accepting paper-side handoff consumption.

## LaTeX Hard Rules
- This paper scaffold uses vendored `cumcmthesis.cls` from CUMCMThesis; do not edit the class file during normal paper work.
- Team/problem metadata lives in `metadata.tex`.
- Body content lives under `sections/`.
- Figures must include `\caption{}` and `\label{}`.
- Tables must use booktabs style (no vertical lines).
- Important equations must be labelable and referenceable.
- Keep figure/output paths under `../figures/` and `../output/`.

## 数模竞赛论文写作与工程约束

> 本节适用于本项目下所有涉及论文撰写或修改的任务。项目性质是一次性数学建模竞赛作业（高教杯国赛），最终交付物是一篇能通过评审的论文和配套图表，不是一个软件工程项目。任何与“论文最终是否读起来像人写的、是否符合竞赛规范”无关的基础设施建设，都不应该发生。

### 工程层面禁令

- 论文 worker 不搭建、不维护任何形式的验收流水线、acceptance check 脚本、`model_manifest.json`、SHA256 校验或 Docker 镜像引用体系。既有 repo harness 只供主脑内部调度和验收，不属于论文内容。
- 即便内部状态或工具用于核对数值一致性，也绝不能在论文正文、图注、表格、附录中留下痕迹。容器名、文件路径、哈希值、JSON/CSV 文件名一律不得出现在最终 PDF 文本里。
- “确保改动前后关键数值一致”只需人工挑选关键数字对比一次，不得为论文修改任务新增自动化核对脚本。
- 不使用面向对象封装，不写单元测试，不做命令行参数解析，不做复杂异常处理。论文配套代码只需能跑通、出结果、画图。
- 不主动新增脚本文件、构建流程或工具链，除非用户明确要求。

### 写作层面禁令

1. **免责句去重复化**：全文对“这是模拟数据 / 不代表真实统计 / 不能解释为因果”等声明，只允许在“模型假设”与“局限与改进”两节各出现一次。其余每张图、每张表后面不得再单独跟一句否定或限定句。陈述结果使用肯定语气，不要写一个结论就否定一次。
2. **数值精度与数据性质一致**：模拟数据不保留六位小数的伪精度。正文中比例、权重和统计量保留 2-3 位小数，百分比保留 1 位，距离保留 1 位，p 值保留 3 位；附录复现参数表可以保留原始精度。
3. **禁止限定词强迫性重复**：同一限定词（如“情景”“代理”）全文出现不应超过约 10 次。第一次完整说明含义后，后文使用简称、直接省略或依靠上下文表达。
4. **摘要必须采用叙事三段式**：背景任务、方法路线、核心结论与政策方向。摘要控制在 300-400 字，不堆砌超过 2-3 个具体数值指标，不写成结果汇总表。
5. **公式和符号必须有实际计算支撑**：论文中每个公式和符号都必须在正文中真实代入数值并给出结果。删除没有计算支撑的装饰性公式，改为自然语言，并同步清理符号表中不再使用的符号。
6. **图注与正文分析分离**：图注不超过 20 字，只说明图的内容。详细解读放在图后正文，用“由图可见”或“图 X 表明”等自然衔接展开。
7. **禁止工程痕迹词汇进入图文**：任务编号（如 P1/P2/P3）、baseline、pre-trend gate、canonical、lambda +/-20% 等英文或工程习语，不得出现在图表标题、图例和论文正文中，必须改为面向读者的自然中文表述。
8. **参考文献采用 GB/T 7714 顺序编码格式**：不强行为每条文献补全 DOI/ISBN，不使用 APA/Chicago 等格式夹带 DOI，保持国内竞赛论文常用风格。
9. **章节标题和中文表达统一**：章节标题统一为名词性短语。全文排查翻译腔和生硬限定句，改为自然中文学术表达；标点使用中文全角标点。

### 论文修改执行顺序

每次论文修改严格按以下顺序执行：

1. 改写；
2. 重新编译；
3. 人工抽查若干关键数值，不新增脚本；
4. 按上述九条逐项自查。

### 文风自我认知边界

- Agent 对自己默认生成文本的“AI 腔”没有可靠的第三方视角，不能自行证明文本已经自然或像人写作。
- 完成后不得声称“已消除 AI 痕迹”或“已通过人性化审查”。只报告具体改写了哪些句子、删除了哪些内容、逐项完成了哪些检查。
- 最终回传必须明确提示用户：**请本人通读一遍摘要和引言部分，凭直觉判断文风是否自然。** 这一步不能由 Agent 代替，也不得暗示已经不再需要人工通读。

## 数学建模论文质量规范

本节是所有论文撰写、论文修改、论文图表、引用审查和论文质量审查任务的共同提示词。凡涉及“要不要写进去”的取舍，统一遵循：题目明确要求的内容、能够体现模型亮点并形成区分度的内容应保留；题目已给出的信息、常识性内容和某类模型的泛泛背景知识不应重复加入。

### 论文结构

- **摘要**按“背景描述、逐问题描述与结果、创新点、结论、关键词”组织。背景控制在 3-5 行；问题描述使用“针对问题 X，提出 XXX 模型，得出 XXX 结果，从而计算出或验证了 XXX”的叙事逻辑；创新点独立成段；关键词为 4-6 个并独立成行。
- 摘要必须覆盖研究目的、建模思想或模型归类、求解思路或主要算法、主要数值结果、模型检验和结论，但不得写成结果汇总表。
- **问题重述**不得复制题目原文，必须用自己的语言表达目的和问题关系；**问题分析**应说明思考过程、关键猜想、模型选择理由及模型与问题的契合点，可与模型建立合并。
- **模型假设**是全文论证的基础。题目已给出的条件不能直接充当假设；假设建议保持 5-10 条且相互独立，每条都必须在正文中实际使用并与结果一致。
- **符号说明**单独成节，只列全文实际使用的符号及含义。
- **模型建立与求解**可采用“先建立全部模型再统一求解”或“按问题分别建立并求解”。推荐使用“问题一的建模与求解 / 模型建立 / 模型求解”的分级结构。
- **结果分析与检验**至少说明验证方式：真实数据比对、不同模型对比或改变假设观察结果变化；在有依据时补充模型优缺点、改进方向和对题目深层问题的分析。
- **参考文献**按竞赛要求和 GB/T 7714 顺序编码格式列出全文引用来源。
- **附录**可放与正文完全一致的程序代码、正文使用的原始数据、长表格和多余图片；附录排版必须与正文一致，程序代码必须与提交支撑材料一致。

### 写作质量红线

- 不反复使用“需要注意的是”“受限于”“仅供参考”等免责声明式套话。
- 数值精度必须与数据来源一致；若为模拟或示例数据，不输出六位小数的伪精度。
- 不把 Docker 容器名、SHA256 哈希、文件路径、日志片段、任务编号、JSON/CSV 文件名或其他开发过程产物写入正文、图表、图注或附录。
- 每个公式必须在正文中实际代入、计算或求解；没有计算支撑的装饰性公式必须删除，并同步清理无用符号。
- 语言要像正常竞赛论文：避免机械对仗，避免每段都用“首先、其次、最后”开头，避免重复使用相同的总结句式。

### 修改后的最小自查

完成论文修改后，按“改写 -> 重新编译 -> 人工抽查关键数值 -> 按本文结构与红线逐项自查”的顺序执行。Agent 不得自称已经消除 AI 痕迹或通过人性化审查；最终回传必须列出具体改写和删除内容，并再次提示用户本人通读摘要与引言，凭直觉判断文风是否自然。

## Runtime State Contract
- 主脑 owns full `../../MEMORY.md`.
- 论文脑 only updates `## Blockers`, `## Next Actions`, `## Handoff Index` when explicitly needed or when it is the only active session.
- Preserve section order and line budget (<=120).

## Execution Rules
- Hard rule: run paper build tools inside Docker container only.
- Never run project LaTeX toolchain directly on host.
- Use `bash ../../scripts/paper.sh build` or an equivalent command derived from `project/paper/runtime/paper.env`.
- Before writing or compiling, treat `../../.env` and `runtime/paper.env` as machine truth, and use `../spec/runtime_contract.md` plus `spec/paper_runtime_contract.md` as rendered mirrors.
- 论文 worker 不为正文修改新增 validator、acceptance checker 或其他工具。既有 repo validator 由主脑按需运行，不属于论文修改默认步骤。

## Failure Recovery
- If validation fails, fix docs first.
- If the active entrypoint changes, update `project/paper/runtime/paper.env`, not this file.
- If handoff or runtime facts are incomplete, stop and return an explicit blocker instead of guessing.
