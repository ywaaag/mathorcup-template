# MathorCup 实例仓库（Human Guide）

这是由 `mathorcup-template` 渲染出的实例仓库。

说明：下面命令假设你是通过 `git clone` 模板后在同一目录原地实例化，因此 `scripts/` 仍在仓库内；如果只是模板维护者用 `--target` 渲染出的临时目录，请回到模板源仓库执行脚本并显式传 `--target <dir>`。

## 1. 先看什么

- `AGENTS.md`
- `project/paper/AGENTS.md`
- `MEMORY.md`
- `project/spec/runtime_contract.md`
- `project/spec/multi_agent_workflow_contract.md`
- `project/workflow/prompt_template_library.md`

说明：
- `AGENTS / MEMORY / handoff / review / retrospective` 主要给 Agent 读写。
- 本 README 主要给人类确认实例化、运行、协作和验收路径。

## 2. 初始化一个新实例

```bash
bash scripts/instantiate.sh {{COMPETITION_NAME}}
```

默认行为：

- 从 `scaffold/` 渲染实例文件。
- 生成 `.env` 和 `project/paper/runtime/paper.env`。
- 在渲染校验通过后删除模板 `.git`，重新 `git init`，创建当前比赛的初始提交。
- 继续执行容器 bootstrap / 依赖安装。

如果你希望保留模板仓库 Git 历史，显式使用：

```bash
bash scripts/instantiate.sh {{COMPETITION_NAME}} --keep-template-git
```

常用变体：

```bash
# 只渲染合同与运行配置，不碰容器
bash scripts/instantiate.sh {{COMPETITION_NAME}} --render-only

# 只渲染并直接生成干净 Git 初始提交
bash scripts/instantiate.sh {{COMPETITION_NAME}} --render-only --reset-git

# 容器已存在，只补依赖
bash scripts/setup.sh --deps-only

# 仅重置 runtime state，不改容器和协议文档
bash scripts/setup.sh --reset-state
```

### 2.1 默认容器运行基线

实例默认复用的镜像与权限配置以根目录 `.env` 为准，初始渲染值是：

- `IMAGE_NAME={{IMAGE_NAME}}`
- `CONTAINER_RUNTIME={{CONTAINER_RUNTIME}}`
- `CONTAINER_GPUS={{CONTAINER_GPUS}}`
- `CONTAINER_PRIVILEGED={{CONTAINER_PRIVILEGED}}`
- `CONTAINER_USER={{CONTAINER_USER}}`
- `CONTAINER_GRANT_SUDO={{CONTAINER_GRANT_SUDO}}`
- `INSTANCE_ID={{INSTANCE_ID}}`
- `CONTAINER_NAME={{CONTAINER_NAME}}`
- `JUPYTER_PORT_MODE={{JUPYTER_PORT_MODE}}`
- `RSTUDIO_PORT_MODE={{RSTUDIO_PORT_MODE}}`

这意味着默认容器会以高权限模式启动，并把工程目录挂到 `/workspace/mathorcup`。

容器名包含持久化的 `INSTANCE_ID`，所以不同目录即使使用相同比赛名，也不会误复用同一个容器。Jupyter / RStudio host 端口默认自动选择并写回 `.env`；容器内部端口仍为 `8888/8787`。

如果 `.env` 中端口模式是 `fixed`，端口冲突时 bootstrap 会失败并显示占用者；如果是 `auto`，bootstrap 会选择新端口并原子更新 `.env`。Docker labels 和实际 project bind mount 必须与当前实例一致，否则脚本拒绝复用或删除该容器。

当前 reference image 的已验证内部环境基线：

- `Python 3.12.12`
- `pip 24.1.2`
- `latexmk 4.83`
- `XeTeX (TeX Live 2023/Debian)`
- `R 4.5.1`
- `sudo` 免密可用
- `biber`
- `fd`
- `tree`
- `yq`
- `qpdf`
- `poppler-utils`
- `Noto CJK / WenQuanYi / AR PL` 中文字体
- matplotlib 中文绘图 helper：`project/src/common/plot_style.py`
- `PyPDF2`
- `PyMuPDF`
- R packages: `ompr`, `ROI`
- `biblatex`（通过 TeX Live 2023 user tree）

已知注意事项：

- 如果不是从 reference image 启动，或镜像内部 baseline 漂移，再执行 `bash scripts/install_deps.sh`
- 如果要跨机器重建同等 runtime baseline，参考模板源仓库的 `docker/Dockerfile.runtime`

## 3. 关键文件分层

- `MEMORY.md`
  - 唯一 runtime state board
- `project/spec/runtime_contract.md`
  - 当前运行事实总入口
- `project/paper/runtime/paper.env`
  - paper active entrypoint/build target 的单一事实源
- `project/spec/multi_agent_workflow_contract.md`
  - 主脑 / 代码脑 / 论文脑 / 杂务 Agent 协作协议
- `project/workflow/prompt_template_library.md`
  - 任务包、预检查、compile-only、最小改动、复盘、验收模板
- `project/output/handoff/`
  - 代码脑向论文脑交接的索引化资产
- `project/output/review/`
  - worker 结构化回传记录
- `project/output/retrospectives/`
  - 经验复盘与 lesson learned

## 4. 常用命令

```bash
# 渲染或刷新合同文档
bash scripts/render_templates.sh

# 创建/启动容器
bash scripts/bootstrap_container.sh

# 安装竞赛依赖
bash scripts/install_deps.sh

# 校验 agent 协议与 runtime config
bash scripts/validate_agent_docs.sh

# 环境诊断
bash scripts/doctor.sh

# 论文编译
bash scripts/paper.sh build

# 生成任务包骨架
bash scripts/make_task_packet.sh paper

# 检查 codex exec 是否可用
bash scripts/exec_healthcheck.sh

# 用 codex exec 跑一个非交互 worker
bash scripts/run_exec_worker.sh --task TASK_REVIEW_CONSISTENCY --owner exec_review
```

## 5. 推荐协作顺序

1. 主脑先读 `AGENTS.md`、`MEMORY.md`、runtime/workflow contracts。
2. 主脑决定当前唯一目标，并从 `project/workflow/TASK_CONTRACT_TEMPLATE.json` 准备领域任务合同。
3. 若走多会话 relay，就先 `dispatch_task.sh` 再把 packet 发给另一个会话。
4. 若走 `codex exec`，就先 `exec_healthcheck.sh`，再用 `run_exec_worker.sh` 执行。
5. 无论哪种 worker backend，回传都要落到 feedback；retrospective 按任务风险 policy 执行。
6. 代码脑产出图表、CSV、handoff。
7. 论文脑只消费已索引 handoff 和当前 active paper entrypoint。
8. 编译/验收杂务 Agent 只做 compile 和 host-visible acceptance。
9. 重要踩坑点写入 `project/output/retrospectives/`。

## 6. 人类如何调用主脑工作流

先看实例状态：

```bash
bash scripts/doctor.sh
bash scripts/main_brain_summary.sh
bash scripts/list_open_tasks.sh --open-only
```

派发一个任务：

```bash
bash scripts/configure_task_contract.sh --task TASK_REVIEW_CONSISTENCY --file /tmp/review_contract.json
bash scripts/dispatch_task.sh --task TASK_REVIEW_CONSISTENCY --owner alice
```

领域任务在 `task_contract.prepared=false` 时会拒绝派发，避免 Agent 在缺少题目输入、参数、算法边界和精确验收标准时开工。

把生成的 task packet 发给另一个 Agent 会话，或用 `codex exec` 执行：

```bash
bash scripts/exec_healthcheck.sh
bash scripts/run_exec_worker.sh --task TASK_REVIEW_CONSISTENCY --owner exec_review
```

worker 回传后由主脑验收：

```bash
bash scripts/show_task.sh --task TASK_REVIEW_CONSISTENCY
bash scripts/list_history.sh --task TASK_REVIEW_CONSISTENCY
bash scripts/check_worker_feedback.sh --task TASK_REVIEW_CONSISTENCY
bash scripts/close_task.sh --task TASK_REVIEW_CONSISTENCY --to review
bash scripts/close_task.sh --task TASK_REVIEW_CONSISTENCY --to done --accepted-by main_brain
```

如果多个 worker 结论不一致，先生成裁决草案：

```bash
bash scripts/adjudicate_task.sh --task TASK_REVIEW_CONSISTENCY
```

## 7. paper active entrypoint 怎么改

只改一个文件：

```bash
project/paper/runtime/paper.env
```

最常改的字段：
- `PAPER_ACTIVE_ENTRYPOINT`
- `PAPER_BUILD_DIR`
- `PAPER_TEXINPUTS`
- `PAPER_ACCEPT_PDF`
- `PAPER_ACCEPT_LOG`

改完后执行：

```bash
bash scripts/validate_agent_docs.sh
```

## 7. 协作模式怎么选

- 多会话 relay：
  - 主脑 dispatch packet，再手工贴给另一个会话
- `codex exec` worker：
  - 主脑用 `run_exec_worker.sh` 直接启动一个非交互 worker
- sub-agent delegation：
  - 如果环境支持，也仍然应该复用同一套 registry / packet / feedback / close 流程

三种模式都共享同一套文档和模板：
  - `MEMORY.md`
  - runtime contract
  - workflow contract
  - prompt-template library
  - handoff / review / retrospective directories
- 主脑仍然负责汇总共识；worker 只回传结构化结果。

## 8. 最低限度验收

```bash
bash scripts/validate_agent_docs.sh
bash scripts/doctor.sh
```

如果是 paper 任务，再补：

```bash
bash scripts/paper.sh build
```
