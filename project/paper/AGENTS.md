# AGENTS.md — Template Paper Placeholder Scope

## Scope

- This `project/paper/` tree in the template repo is a render target placeholder.
- The instance-facing source of truth for paper-side protocol and contracts lives under:
  - `scaffold/project/paper/`

## Editing Rule

- If you want future instantiated repos to change, edit `scaffold/project/paper/`.
- Only edit the live `project/paper/` tree here when you are explicitly working on a rendered smoke-test target.
